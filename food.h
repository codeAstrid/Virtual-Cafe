#include <vector>
#include <string>

// Declare the foodOptions vector
extern std::vector<std::pair<std::string, double> > foodOptions;

std::vector<std::pair<std::string, double> > getFoodOptions();
